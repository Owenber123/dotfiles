# run-shell "echo '✅ Loaded .tmux.conf' >> ~/tmux_load_log.txt"

# set -g prefix C-a

# Remove the old prefix
# unbind C-b

# Send Ctrl+a to applications by pressing it twice
# bind C-a send-prefix

set -g mouse on
set -g status-left-length 100
set -g status-left ' #[bold][#S] '

# Default Term
set -g default-terminal "tmux-256color"

# Pass OSC52 clipboard data through tmux
set -g set-clipboard on

# Allow OSC52 escape passthrough (tmux 3.3+)
set -g allow-passthrough on

# Enable truecolor (optional, for Neovim color correctness)
set -sa terminal-overrides ',xterm*:Tc'

# unbind C-b
# set -g prefix C-Space
# bind C-Space send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch window
bind -n M-H previous-window
bind -n M-L next-window

set -g @catppuccin_flavour 'mocha'
# set -g @tokyonight_style 'night'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
# set -g @plugin 'catppuccin/tmux'
# set -g @plugin 'folke/tokyonight.nvim'
set -g @plugin 'tmux-plugins/tmux-yank'

run '~/.tmux/plugins/tpm/tpm'

# set-option -g status-style "bg=red,fg=blue"
set -g @catppuccin_window_tabs_enabled off


# set vi-mode
set-window-option -g mode-keys vi
# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
